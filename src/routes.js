import React from "react";
import { Route, Switch } from "react-router-dom";

const Routes = () => {
  return (
    <Switch>
      {/* Routes */}
      {/* Routes */}
      {/* Routes */}
    </Switch>
  );
};

export default Routes;
