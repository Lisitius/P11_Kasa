import React from "react";
import { Routes } from "react-router-dom";

const FileRoute = () => {
  return (
    <Routes>
      {/* Routes */}
      {/* Routes */}
      {/* Routes */}
    </Routes>
  );
};

export default FileRoute;
